# Platform-independent requirements for tfm-stockformer
# Requires Python 3.11 (pytorch-wavelets is not compatible with 3.12)
# Run from project root with: pip install -r requirements.txt
#
# For GPU training (Colab / Linux):
#   CUDA 11.8: pip install torch==2.7.1+cu118 --index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.8: pip install torch==2.10.0+cu128 --index-url https://download.pytorch.org/whl/cu128

torch>=2.0
PyWavelets>=1.6
tensorboard>=2.14,<3
scikit-learn>=1.1.2
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
tqdm>=4.62
statsmodels>=0.14.1
pytorch-wavelets==1.3.0
setuptools<81  # pytorch-wavelets uses pkg_resources, removed in setuptools>=81
networkx>=3.2
pandas>=2.0
lxml>=4.9          # required by pandas.read_html (S&P500 ticker list from Wikipedia)
pytest>=7.0,<8
yfinance>=0.2.50
pyarrow>=12.0
gensim>=4.0              # required by GraphEmbedding (Struc2Vec Word2Vec walks)
fastdtw>=0.3.4          # required by GraphEmbedding Struc2Vec
# GraphEmbedding (Struc2Vec — LINE/SDNE not needed):
#   pip install git+https://github.com/shenweichen/GraphEmbedding.git --no-deps
# After install, patch ge/models/__init__.py — wrap LINE/SDNE imports in try/except
# (they require tensorflow/deepctr which we don't use)
streamlit>=1.35,<=1.50
plotly>=5.18,<7
