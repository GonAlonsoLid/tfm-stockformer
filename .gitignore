# --- Python ---
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
venv/
.venv/

# --- Jupyter ---
.ipynb_checkpoints/

# --- Data (large; sourced externally, not versioned) ---
data/

# --- Training logs / scratch (regenerable) ---
runs/
log/
temp_struc2vec/

# --- Result caches (regenerable; canonical numbers live in results/*.csv) ---
results/figures/
results/_wf_preds_*.npz

# --- LaTeX build artifacts ---
*.aux
*.log
*.toc
*.out
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.lof
*.lot
*.run.xml
*.bcf

# --- Local-only (kept on disk, intentionally out of the repo):
#     demo (streamlit/notebooks/Backtest), internal planning docs, external
#     reference PDFs, orphaned DGX bundle, and the reference TFG ---
_local/

# --- Internal planning / agent state (not part of the thesis) ---
.planning/
.claude/

# --- OS / IDE ---
.DS_Store
Thumbs.db
.vscode/settings.json
