---
phase: 03-model-training
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_model_training.py
autonomous: true
requirements:
  - MODEL-01
  - MODEL-02

must_haves:
  truths:
    - "pytest collects all MODEL-01 and MODEL-02 test stubs without error"
    - "All stubs are marked xfail(strict=False) so the suite passes before implementation"
    - "Test names match the verification map in 03-VALIDATION.md exactly"
  artifacts:
    - path: "tests/test_model_training.py"
      provides: "Failing stubs for config validation, dataset loading, forward pass, and inference script checks"
      exports: ["test_config_file_exists", "test_config_fields_present", "test_dataset_loads", "test_stockformer_forward_pass", "test_inference_script_exists", "test_inference_script_args"]
  key_links:
    - from: "tests/test_model_training.py"
      to: "config/Multitask_Stock_SP500.conf"
      via: "configparser read + assertion on file existence"
      pattern: "config.*Multitask_Stock_SP500\\.conf"
    - from: "tests/test_model_training.py"
      to: "scripts/run_inference.py"
      via: "os.path.exists assertion"
      pattern: "run_inference\\.py"
---

<objective>
Create the Wave 0 test scaffold for Phase 3. Every test in `tests/test_model_training.py` must be an xfail stub today so the full pytest suite continues to collect and pass. Implementation plans (03-02, 03-03) will turn these stubs green.

Purpose: Nyquist compliance — automated feedback is available from the first task of every subsequent plan.
Output: `tests/test_model_training.py` with six stubs covering MODEL-01 and MODEL-02.
</objective>

<execution_context>
@/Users/gonzaloalonsolidon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gonzaloalonsolidon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-model-training/03-CONTEXT.md
@.planning/phases/03-model-training/03-VALIDATION.md

<interfaces>
<!-- Existing fixtures available in tests/conftest.py -->

```python
# tests/conftest.py — already present, no changes needed for Wave 0

@pytest.fixture
def project_root():
    return PROJECT_ROOT  # absolute path to repo root

@pytest.fixture
def config(project_root):
    cfg = configparser.ConfigParser()
    cfg.read(os.path.join(project_root, 'config', 'Multitask_Stock.conf'))
    return cfg
```

<!-- Validation map from 03-VALIDATION.md — these exact test function names MUST be used -->
<!--
  test_config_file_exists         — MODEL-01 — config/Multitask_Stock_SP500.conf exists
  test_config_fields_present      — MODEL-01 — all four INI sections + required keys present
  test_dataset_loads              — MODEL-01/02 — StockDataset loads without crashing (smoke)
  test_stockformer_forward_pass   — MODEL-02 — Stockformer forward pass returns correct shape
  test_inference_script_exists    — MODEL-02 — scripts/run_inference.py file exists
  test_inference_script_args      — MODEL-02 — run_inference.py accepts --config and --checkpoint
-->

<!-- Phase 2 decision — already in STATE.md:
  "xfail stubs use strict=False so suite collects without failing until implementation lands"
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write xfail test stubs for MODEL-01 and MODEL-02</name>
  <files>tests/test_model_training.py</files>
  <action>
Create `tests/test_model_training.py` with the following six test stubs. All stubs use `@pytest.mark.xfail(strict=False, reason="...")` so the suite collects and passes before implementation. Do NOT import implementation modules at module level — imports go inside each test function body so the file loads without errors even when scripts are missing.

Required stubs (exact function names — match 03-VALIDATION.md):

1. `test_config_file_exists(project_root)` — assert `config/Multitask_Stock_SP500.conf` exists
2. `test_config_fields_present(project_root)` — load config with configparser, assert all four sections ([file], [data], [train], [param]) exist and required keys are present: traffic, indicator, adj, adjgat, model, log, alpha_360_dir, output_dir, tensorboard_dir, T1, T2, train_ratio, val_ratio, test_ratio, cuda, max_epoch, batch_size, learning_rate, seed, layers, heads, dims, samples, wave, level
3. `test_dataset_loads(project_root)` — assert config file exists (placeholder for dataset loading — full test requires real data on disk; mark as xfail with reason "requires Phase 2 data on disk")
4. `test_stockformer_forward_pass()` — import Stockformer from Stockformermodel.Multitask_Stockformer_models, construct with (infea=71, hidden=128, outfea_class=2, outfea_regress=1, L=2, h=1, d=128, s=1, T1=20, T2=2, device=cpu), run forward with a random input tensor of shape (batch=2, N=5, T1+T2=22) and assert output is a tuple with two tensors; mark xfail with reason "requires full model setup verification"
5. `test_inference_script_exists(project_root)` — assert `scripts/run_inference.py` exists
6. `test_inference_script_args(project_root)` — read `scripts/run_inference.py` as text and assert both `--config` and `--checkpoint` appear in the source; mark xfail with reason "requires run_inference.py to be created"

Module-level imports allowed: `pytest`, `os`, `sys`, `configparser`. Everything else inside test bodies.

The `project_root` fixture is already in `tests/conftest.py` — use it directly, no changes to conftest needed.
  </action>
  <verify>
    <automated>cd /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer && python -m pytest tests/test_model_training.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    `pytest tests/test_model_training.py -x -q` exits 0 (all stubs collected and marked xfail). Six test functions exist with exact names matching 03-VALIDATION.md. Full suite `pytest tests/ -q` also exits 0.
  </done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions and all new stubs collect cleanly:
```
python -m pytest tests/ -q
```
Expected: all tests pass or xfail; 0 errors; 0 failures.
</verification>

<success_criteria>
- `tests/test_model_training.py` exists with six test stubs
- All six use `@pytest.mark.xfail(strict=False)`
- `pytest tests/ -q` exits 0
- Function names match 03-VALIDATION.md exactly
</success_criteria>

<output>
After completion, create `.planning/phases/03-model-training/03-01-SUMMARY.md` documenting:
- File created: tests/test_model_training.py
- Six stub names
- Any deviations from the plan
</output>
