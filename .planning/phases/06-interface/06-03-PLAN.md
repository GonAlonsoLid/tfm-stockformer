---
phase: 06-interface
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified: []
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "streamlit run app.py opens a browser tab with page title 'Stockformer S&P500 Dashboard'"
    - "Sidebar renders with 7 controls: config path, output dir, top-k, txn cost, benchmark, start date, end date"
    - "Main area shows placeholder instructions before any run"
    - "Run Pipeline button is visible and styled as primary (accent color)"
    - "After successful run: equity curve chart, metrics table, heatmap appear in main area"
    - "Equity curve shows Portfolio (blue) vs SPY (gray dotted) with hover tooltips"
    - "Metrics table has 8 columns with formatted values"
    - "Heatmap shows top-K stocks with red-white-green diverging colorscale"
  artifacts:
    - path: "app.py"
      provides: "Running Streamlit app"
  key_links:
    - from: "browser UI"
      to: "app.py main()"
      via: "streamlit run app.py"
      pattern: "streamlit run"
---

<objective>
Human verification checkpoint: launch the Streamlit app against existing output files and confirm all four UI requirements are visually satisfied.

Purpose: Automated tests confirm function contracts but cannot verify the browser rendering, hover tooltips, spinner behavior, or visual aesthetics. This checkpoint confirms the app is shippable as a thesis demo tool.
Output: Signed-off UI that satisfies all phase success criteria.
</objective>

<execution_context>
@/Users/gonzaloalonsolidon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gonzaloalonsolidon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-interface/06-CONTEXT.md
@.planning/phases/06-interface/06-UI-SPEC.md
@.planning/phases/06-interface/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite + verify app.py imports cleanly</name>
  <files>tests/test_app.py, app.py</files>
  <read_first>
    - app.py (confirm all exported symbols exist)
    - tests/test_app.py (confirm 7 tests pass)
  </read_first>
  <action>
    Run the complete test suite and confirm:

    1. `python -m pytest tests/test_app.py -v` — all 7 tests pass (not xfail)
    2. `python -m pytest tests/ -q` — full suite green, no regressions
    3. `python -c "import app; print(app.DEFAULT_CONFIG_PATH, app.DEFAULT_TOP_K)"` — prints expected values

    If any test fails, fix app.py before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer && python -m pytest tests/test_app.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_app.py -v` exits 0 with 7 PASSED lines
    - `python -m pytest tests/ -q` exits 0
    - `python -c "import app; print(app.DEFAULT_TOP_K)"` outputs "10"
  </acceptance_criteria>
  <done>All 7 test_app.py tests pass; full suite green; import app works without Streamlit server</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — launch app and confirm all 4 UI requirements</name>
  <what-built>
    Complete Streamlit dashboard (app.py) with:
    - Sidebar: 7 controls (config path, output dir, top-k, txn cost, benchmark, start date, end date) + Run Pipeline button
    - Main area: initial placeholder → equity curve chart + metrics table + prediction heatmap after run
    - Dark theme via .streamlit/config.toml
    - Live subprocess log streaming during pipeline execution
  </what-built>
  <how-to-verify>
    STEP 1 — Launch the app:
    ```
    cd /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer
    streamlit run app.py
    ```
    A browser tab should open at http://localhost:8501.

    STEP 2 — Verify initial state (UI-01):
    - [ ] Page title "Stockformer S&P500 Dashboard" appears
    - [ ] Sidebar shows "Pipeline Settings" section with config path and output dir fields
    - [ ] Sidebar shows "Backtest Parameters" section with Top-K (10), txn cost (10), benchmark (SPY), start date, end date
    - [ ] "Run Pipeline" button is visible and styled with accent color (#4C9BE8)
    - [ ] Main area shows info box: "Configure settings to run your first analysis"

    STEP 3 — Run against existing output (UI-02, UI-03, UI-04):
    Verify the output directory field shows "output/Multitask_output_SP500_2018-2024" as default, then click "Run Pipeline".
    - [ ] Spinner appears: "Running inference and backtest — this may take several minutes..."
    - [ ] Live log lines scroll in a code block below the spinner
    - [ ] After completion, main area shows three sections: Equity Curve, Performance Metrics, Prediction Heatmap

    STEP 4 — Verify equity curve (UI-02):
    - [ ] Line chart shows two traces: "Portfolio" (blue solid) and "SPY" (gray dotted)
    - [ ] Both lines start at approximately 1.0 on the y-axis
    - [ ] Hovering a point shows date + portfolio value + SPY value with "x" suffix
    - [ ] Chart title: "Cumulative Return: Portfolio vs SPY"

    STEP 5 — Verify metrics table (UI-03):
    - [ ] Table appears below the equity curve
    - [ ] Has 8 columns: Ann. Return, Total Return, Max Drawdown, Alpha (Ann.), Sharpe, Beta, Top-K, Days
    - [ ] Return/drawdown/alpha columns show percentage values
    - [ ] Sharpe and Beta show 2-decimal numbers

    STEP 6 — Verify prediction heatmap (UI-04):
    - [ ] Heatmap appears below the metrics table
    - [ ] Shows top-K stocks (default 10) on y-axis with ticker symbols
    - [ ] Red-white-green diverging colorscale (negative=red, zero=white, positive=green)
    - [ ] Hovering a cell shows ticker name, day, and score value
    - [ ] Chart title: "Prediction Scores — Top-10 Stocks" (or matching K value)

    STEP 7 — Verify dark theme:
    - [ ] Background is dark (#0E1117 equivalent)
    - [ ] Sidebar background is slightly lighter (#1E2130 equivalent)
    - [ ] Overall appearance is "clean and presentable" for a thesis demo
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checkboxes pass.
    Or describe any visual issues (e.g., "heatmap ticker labels are wrong", "equity curve starts at 0 not 1.0") and the executor will fix app.py.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 6 complete when:
1. pytest tests/test_app.py passes 7/7 tests
2. pytest tests/ passes full suite
3. Human approves the browser UI at all 4 UI checkpoints
4. `streamlit run app.py` launches without error on a machine with the venv activated
</verification>

<success_criteria>
- UI-01: streamlit run app.py opens browser with date range controls + Run Pipeline button
- UI-02: Equity curve shows portfolio vs SPY cumulative returns (Plotly, interactive hover)
- UI-03: Metrics table shows all 8 backtest_summary.csv columns with formatted values
- UI-04: Heatmap shows top-K stocks × dates with diverging red-white-green colorscale
- All 7 automated tests in tests/test_app.py pass
- Full test suite (pytest tests/) stays green (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/06-interface/06-03-SUMMARY.md`
</output>
