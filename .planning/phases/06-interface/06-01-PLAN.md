---
phase: 06-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_app.py
  - tests/conftest.py
  - .streamlit/config.toml
  - requirements.txt
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "pytest tests/test_app.py -x -q exits 0 with all stubs collected (xfail)"
    - "requirements.txt contains streamlit>=1.35,<=1.50 and plotly>=5.18,<7"
    - ".streamlit/config.toml exists with dark theme tokens"
    - "conftest.py contains backtest_daily_returns_fixture, backtest_summary_fixture, regression_pred_fixture"
  artifacts:
    - path: "tests/test_app.py"
      provides: "xfail stubs covering UI-01 through UI-04 (7 test functions)"
      contains: "test_app_imports, test_sidebar_defaults, test_equity_chart_shape, test_equity_chart_starts_at_one, test_metrics_table_columns, test_heatmap_zmid, test_heatmap_top_k_filter"
    - path: "tests/conftest.py"
      provides: "synthetic CSV fixtures used by test_app.py"
      contains: "backtest_daily_returns_fixture, backtest_summary_fixture, regression_pred_fixture"
    - path: ".streamlit/config.toml"
      provides: "Streamlit dark theme config"
      contains: "primaryColor"
    - path: "requirements.txt"
      provides: "pinned streamlit and plotly dependencies"
      contains: "streamlit"
  key_links:
    - from: "tests/test_app.py"
      to: "app.py"
      via: "import app (guarded with try/except ImportError for pre-implementation)"
      pattern: "import app"
    - from: "tests/conftest.py"
      to: "tests/test_app.py"
      via: "fixture injection"
      pattern: "backtest_daily_returns_fixture"
---

<objective>
Wave 0 setup: create the test scaffold for Phase 6 before any app code exists, install dependencies, and write the Streamlit theme config.

Purpose: Establish the RED phase of TDD — tests fail (xfail) until app.py is implemented. Ensures CI stays green throughout the phase.
Output: tests/test_app.py with 7 xfail stubs, conftest.py augmented with 3 synthetic fixtures, .streamlit/config.toml, and requirements.txt with pinned streamlit + plotly.
</objective>

<execution_context>
@/Users/gonzaloalonsolidon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gonzaloalonsolidon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-interface/06-CONTEXT.md
@.planning/phases/06-interface/06-RESEARCH.md
@.planning/phases/06-interface/06-UI-SPEC.md
@.planning/phases/06-interface/06-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_app.py with xfail stubs + augment conftest.py with synthetic fixtures</name>
  <files>tests/test_app.py, tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (existing fixtures, sys.path setup pattern)
    - tests/test_backtest.py (xfail pattern used in Phase 5)
    - .planning/phases/06-interface/06-RESEARCH.md (§Validation Architecture → Phase Requirements → Test Map)
    - .planning/phases/06-interface/06-VALIDATION.md (§Per-Task Verification Map)
  </read_first>
  <behavior>
    - test_app_imports: import app succeeds (xfail until app.py exists)
    - test_sidebar_defaults: app exposes DEFAULT_CONFIG_PATH="config/Multitask_Stock_SP500.conf", DEFAULT_OUTPUT_DIR="output/Multitask_output_SP500_2018-2024", DEFAULT_TOP_K=10
    - test_equity_chart_shape: build_equity_chart(df) returns go.Figure with exactly 2 traces
    - test_equity_chart_starts_at_one: first y-value of both traces equals 1.0
    - test_metrics_table_columns: format_metrics_table(df) returns DataFrame with columns annualized_return, total_return, max_drawdown, alpha_annualized, sharpe_ratio, beta, top_k, n_days
    - test_heatmap_zmid: build_heatmap(pred_df, tickers, k) returns go.Figure whose first trace is go.Heatmap with zmid==0
    - test_heatmap_top_k_filter: build_heatmap with k=3 returns heatmap with exactly 3 y-axis values
  </behavior>
  <action>
    STEP 1 — Augment tests/conftest.py (append after the existing feature_matrix_fixture):

    Add these three fixtures:

    ```python
    @pytest.fixture
    def backtest_daily_returns_fixture():
        """Synthetic backtest_daily_returns.csv data: 50 rows of (date, portfolio_return, spy_return)."""
        dates = pd.bdate_range(start="2023-01-03", periods=50)
        np.random.seed(7)
        return pd.DataFrame({
            "date": dates.strftime("%Y-%m-%d"),
            "portfolio_return": np.random.randn(50) * 0.01,
            "spy_return": np.random.randn(50) * 0.008,
        })

    @pytest.fixture
    def backtest_summary_fixture():
        """Synthetic backtest_summary.csv data: one-row DataFrame with all 8 required columns."""
        return pd.DataFrame([{
            "annualized_return": 0.1234,
            "sharpe_ratio": 1.23,
            "max_drawdown": -0.0987,
            "alpha_annualized": 0.0456,
            "beta": 1.05,
            "top_k": 10,
            "n_days": 50,
            "total_return": 0.0654,
        }])

    @pytest.fixture
    def regression_pred_fixture():
        """Synthetic regression_pred_last_step.csv data: DataFrame shape (50, 10), headerless."""
        np.random.seed(13)
        return pd.DataFrame(np.random.randn(50, 10) * 0.05)
    ```

    STEP 2 — Create tests/test_app.py from scratch:

    ```python
    """Tests for app.py — Phase 6 Interface.

    All tests are xfail(strict=False) until app.py is implemented.
    Imports inside test bodies prevent module-level ImportError when app.py does not exist yet.
    """
    import pytest
    import numpy as np
    import pandas as pd


    # ── UI-01: app.py exists and exports expected constants ───────────────────────

    @pytest.mark.xfail(strict=False, reason="app.py not yet implemented")
    def test_app_imports():
        """app.py must exist at project root and import without error."""
        import app  # noqa: F401


    @pytest.mark.xfail(strict=False, reason="app.py not yet implemented")
    def test_sidebar_defaults():
        """app.py must export DEFAULT_CONFIG_PATH, DEFAULT_OUTPUT_DIR, DEFAULT_TOP_K."""
        import app
        assert app.DEFAULT_CONFIG_PATH == "config/Multitask_Stock_SP500.conf"
        assert app.DEFAULT_OUTPUT_DIR == "output/Multitask_output_SP500_2018-2024"
        assert app.DEFAULT_TOP_K == 10


    # ── UI-02: equity curve chart ─────────────────────────────────────────────────

    @pytest.mark.xfail(strict=False, reason="app.py not yet implemented")
    def test_equity_chart_shape(backtest_daily_returns_fixture):
        """build_equity_chart(df) must return a go.Figure with exactly 2 traces."""
        import plotly.graph_objects as go
        from app import build_equity_chart
        df = backtest_daily_returns_fixture
        fig = build_equity_chart(df)
        assert isinstance(fig, go.Figure)
        assert len(fig.data) == 2


    @pytest.mark.xfail(strict=False, reason="app.py not yet implemented")
    def test_equity_chart_starts_at_one(backtest_daily_returns_fixture):
        """Both equity curve traces must start at y=1.0."""
        from app import build_equity_chart
        df = backtest_daily_returns_fixture
        fig = build_equity_chart(df)
        # Both traces start at 1.0 (cumulative return from 1.0)
        assert abs(fig.data[0].y[0] - 1.0) < 1e-6, f"Portfolio trace starts at {fig.data[0].y[0]}, expected 1.0"
        assert abs(fig.data[1].y[0] - 1.0) < 1e-6, f"SPY trace starts at {fig.data[1].y[0]}, expected 1.0"


    # ── UI-03: metrics table ──────────────────────────────────────────────────────

    @pytest.mark.xfail(strict=False, reason="app.py not yet implemented")
    def test_metrics_table_columns(backtest_summary_fixture):
        """format_metrics_table(df) must return a DataFrame with all 8 required columns."""
        from app import format_metrics_table
        result = format_metrics_table(backtest_summary_fixture)
        assert isinstance(result, pd.DataFrame)
        required_cols = {"annualized_return", "total_return", "max_drawdown",
                         "alpha_annualized", "sharpe_ratio", "beta", "top_k", "n_days"}
        assert required_cols.issubset(set(result.columns)), (
            f"Missing columns: {required_cols - set(result.columns)}"
        )


    # ── UI-04: prediction heatmap ─────────────────────────────────────────────────

    @pytest.mark.xfail(strict=False, reason="app.py not yet implemented")
    def test_heatmap_zmid(regression_pred_fixture):
        """build_heatmap must return a go.Figure whose first trace has zmid=0."""
        import plotly.graph_objects as go
        from app import build_heatmap
        pred_df = regression_pred_fixture
        tickers = [f"TICK{i}" for i in range(10)]
        fig = build_heatmap(pred_df, tickers, k=5)
        assert isinstance(fig, go.Figure)
        assert len(fig.data) >= 1
        heatmap_trace = fig.data[0]
        assert isinstance(heatmap_trace, go.Heatmap)
        assert heatmap_trace.zmid == 0


    @pytest.mark.xfail(strict=False, reason="app.py not yet implemented")
    def test_heatmap_top_k_filter(regression_pred_fixture):
        """build_heatmap with k=3 must produce a heatmap with exactly 3 y-axis rows."""
        from app import build_heatmap
        pred_df = regression_pred_fixture
        tickers = [f"TICK{i}" for i in range(10)]
        fig = build_heatmap(pred_df, tickers, k=3)
        heatmap_trace = fig.data[0]
        # z shape: (K, n_days) — y-axis has K entries
        assert len(heatmap_trace.y) == 3, (
            f"Expected 3 y-axis entries (top-3 tickers), got {len(heatmap_trace.y)}"
        )
    ```

    IMPORTANT: imports inside each test body (not at module level) — prevents ImportError when app.py does not exist yet. This follows the established project convention from test_backtest.py and test_compute_ic.py.
  </action>
  <verify>
    <automated>cd /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer && python -m pytest tests/test_app.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_app.py exists
    - tests/test_app.py contains "test_app_imports"
    - tests/test_app.py contains "test_sidebar_defaults"
    - tests/test_app.py contains "test_equity_chart_shape"
    - tests/test_app.py contains "test_equity_chart_starts_at_one"
    - tests/test_app.py contains "test_metrics_table_columns"
    - tests/test_app.py contains "test_heatmap_zmid"
    - tests/test_app.py contains "test_heatmap_top_k_filter"
    - tests/test_app.py contains "xfail" on every test function
    - tests/conftest.py contains "backtest_daily_returns_fixture"
    - tests/conftest.py contains "backtest_summary_fixture"
    - tests/conftest.py contains "regression_pred_fixture"
    - `python -m pytest tests/test_app.py -x -q` exits 0 (all xfail, suite green)
  </acceptance_criteria>
  <done>pytest tests/test_app.py -x -q exits 0 with 7 tests collected (all xfail/skipped, none erroring)</done>
</task>

<task type="auto">
  <name>Task 2: Add .streamlit/config.toml + pin streamlit and plotly in requirements.txt</name>
  <files>.streamlit/config.toml, requirements.txt</files>
  <read_first>
    - requirements.txt (existing pinned packages, formatting style)
    - .planning/phases/06-interface/06-UI-SPEC.md (§Streamlit Theme Configuration — exact TOML values)
    - .planning/phases/06-interface/06-RESEARCH.md (§Standard Stack — streamlit>=1.35,<=1.50 and plotly>=5.18,<7)
  </read_first>
  <action>
    STEP 1 — Create .streamlit/config.toml (create directory if not exists):

    ```toml
    [theme]
    base = "dark"
    backgroundColor = "#0E1117"
    secondaryBackgroundColor = "#1E2130"
    primaryColor = "#4C9BE8"
    textColor = "#E5E7EB"
    font = "sans serif"
    ```

    STEP 2 — Add to requirements.txt (append after the last line):

    ```
    streamlit>=1.35,<=1.50
    plotly>=5.18,<7
    ```

    Rationale for version pins (from RESEARCH.md):
    - streamlit<=1.50: last version supporting Python 3.9 (1.51 dropped it)
    - plotly>=5.18: confirmed go.Heatmap zmid support; <7 avoids breaking API changes
  </action>
  <verify>
    <automated>cd /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer && python -c "import configparser; p = configparser.ConfigParser(); p.read('.streamlit/config.toml'); print(p.sections())" && grep -c "streamlit" requirements.txt && grep -c "plotly" requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - .streamlit/config.toml exists
    - .streamlit/config.toml contains "primaryColor"
    - .streamlit/config.toml contains "#4C9BE8"
    - .streamlit/config.toml contains "#0E1117"
    - .streamlit/config.toml contains "#1E2130"
    - .streamlit/config.toml contains "#E5E7EB"
    - requirements.txt contains "streamlit"
    - requirements.txt contains "plotly"
  </acceptance_criteria>
  <done>.streamlit/config.toml written with all 6 theme tokens; requirements.txt has streamlit and plotly pins</done>
</task>

</tasks>

<verification>
Run full test suite to confirm Wave 0 is green:

```
cd /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer
python -m pytest tests/test_app.py -q
```

Expected: 7 xfail tests, 0 errors, exit code 0.
</verification>

<success_criteria>
- tests/test_app.py has 7 xfail test functions covering UI-01 through UI-04
- tests/conftest.py has 3 new synthetic fixtures for app tests
- .streamlit/config.toml has dark theme with #0E1117 / #1E2130 / #4C9BE8 / #E5E7EB
- requirements.txt pins streamlit>=1.35,<=1.50 and plotly>=5.18,<7
- pytest tests/test_app.py exits 0 (xfail = still green)
</success_criteria>

<output>
After completion, create `.planning/phases/06-interface/06-01-SUMMARY.md`
</output>
