---
phase: 01-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - SETUP.md
autonomous: false
requirements:
  - INFRA-03

must_haves:
  truths:
    - "A developer cloning the repo for the first time can follow SETUP.md and run the smoke test in under 30 minutes"
    - "SETUP.md covers: Python version, venv creation, pip install command, smoke test command, and expected output"
    - "The smoke test command in SETUP.md is the same command verified in Plan 01 (python3 scripts/smoke_test.py)"
  artifacts:
    - path: "SETUP.md"
      provides: "Step-by-step onboarding from fresh clone to passing smoke test"
      min_lines: 40
      contains: "python3 scripts/smoke_test.py"
  key_links:
    - from: "SETUP.md smoke test step"
      to: "scripts/smoke_test.py"
      via: "Exact command documented: python3 scripts/smoke_test.py"
      pattern: "smoke_test\\.py"
---

<objective>
Write SETUP.md — a four-step onboarding guide that takes a developer from fresh clone to passing smoke test in under 30 minutes.

Purpose: INFRA-03 requires that a developer can reproduce the environment from documentation alone. This plan produces that document and verifies it by having a human follow it.
Output: SETUP.md at project root. Checkpoint confirms it works end-to-end.
</objective>

<execution_context>
@/Users/gonzaloalonsolidon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gonzaloalonsolidon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
@.planning/phases/01-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SETUP.md</name>
  <files>SETUP.md</files>
  <action>
Create SETUP.md at the project root. The document must contain exactly four sections in this order:

**Section 1 — Prerequisites**
List what must already be installed before following these instructions:
- Python 3.9 or later (verify: `python3 --version`)
- Git (to clone the repo)
- Note that a CUDA-capable GPU is NOT required for the smoke test; CPU is sufficient

**Section 2 — Clone and create virtual environment**
Exact commands:
```bash
git clone <repo-url>
cd tfm-stockformer
python3 -m venv venv
source venv/bin/activate    # macOS/Linux
# venv\Scripts\activate     # Windows
```

**Section 3 — Install dependencies**
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

Include a note for GPU training:
"For GPU training (CUDA 11.8), replace the torch line in requirements.txt with `torch==2.8.0+cu118` before installing."

**Section 4 — Verify the environment with the smoke test**
```bash
python3 scripts/smoke_test.py
```

Expected output:
```
Running Phase 1 smoke tests...
PASS: config loads and has all required keys
PASS: imports OK — torch 2.8.0, pandas 2.3.3
PASS: Stockformer instantiates successfully

All smoke tests passed. Environment is ready.
```

Explain that this smoke test:
1. Verifies the config file has all required path keys (INFRA-01)
2. Verifies all core packages import correctly (INFRA-02)
3. Instantiates the Stockformer model without training data (confirms PyTorch + pytorch-wavelets work)

**Section 5 — Running the test suite (optional)**
```bash
pytest tests/test_phase1_infra.py -v
```
Note that full training tests require data files from Phase 2.

**Section 6 — Project structure (brief)**
One-paragraph description pointing to:
- `config/Multitask_Stock.conf` — all path and hyperparameter configuration
- `MultiTask_Stockformer_train.py` — training entry point
- `data_processing_script/` — offline data preprocessing scripts (Phase 2)
- `.planning/` — project planning documents

**Section 7 — Troubleshooting**
Three common issues matching the pitfalls identified in research:
1. "ModuleNotFoundError: No module named 'pywt'" — PyWavelets not installed; run `pip install PyWavelets==1.6.0`
2. "ModuleNotFoundError: No module named 'tensorboard'" — tensorboard not installed; run `pip install tensorboard`
3. "FileNotFoundError: /root/autodl-tmp/..." — Path fix from Plan 01 was not applied; check that config/Multitask_Stock.conf has alpha_360_dir key

Do NOT include human dev time estimates, sprint language, or team-oriented language. Keep it direct and imperative.
  </action>
  <verify>
    <automated>test -f /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer/SETUP.md && grep -c "smoke_test.py" /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer/SETUP.md</automated>
  </verify>
  <done>
    - SETUP.md exists at project root
    - Contains "python3 scripts/smoke_test.py" in the verification step
    - Covers prerequisites, venv creation, pip install, smoke test command, and expected output
    - Length at least 40 lines
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify smoke test and SETUP.md end-to-end (INFRA-03)</name>
  <what-built>
    Plan 01 patched all hardcoded paths and fixed requirements.txt. Plan 02 Task 1 wrote SETUP.md. Together these satisfy all three Phase 1 requirements: INFRA-01 (no /root/ paths), INFRA-02 (working requirements.txt), INFRA-03 (setup docs with smoke test).
  </what-built>
  <how-to-verify>
    Follow these steps to verify INFRA-03 end-to-end:

    1. Open a new terminal (do NOT reuse the Claude session's terminal — you want a fresh shell)
    2. Activate the venv: `source venv/bin/activate`
    3. If you want to test a clean install, optionally deactivate and recreate: `python3 -m venv venv_test && source venv_test/bin/activate && pip install -r requirements.txt`
    4. Run the smoke test from the project root:
       `python3 scripts/smoke_test.py`
    5. Confirm you see:
       ```
       PASS: config loads and has all required keys
       PASS: imports OK — torch 2.8.0, pandas 2.3.3
       PASS: Stockformer instantiates successfully
       All smoke tests passed. Environment is ready.
       ```
    6. Run the automated tests:
       `pytest tests/test_phase1_infra.py -v`
    7. Confirm test_no_hardcoded_paths, test_config_has_new_keys, test_torch_importable, test_pywavelets_importable, test_tensorboard_importable all pass.
    8. Optionally open SETUP.md and confirm you could follow it from scratch.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass. Describe any failures with the exact error message so they can be diagnosed and fixed.
  </resume-signal>
  <action>Human follows the verification steps above and confirms the smoke test passes.</action>
  <verify>
    <automated>python3 scripts/smoke_test.py</automated>
  </verify>
  <done>Human confirms: python3 scripts/smoke_test.py prints "All smoke tests passed" and pytest tests/test_phase1_infra.py passes.</done>
</task>

</tasks>

<verification>
Phase 1 is complete when:
1. `grep -rn '/root/autodl-tmp' . --include="*.py" | grep -v ".planning" | grep -v "venv"` returns 0 lines
2. `python3 scripts/smoke_test.py` prints "All smoke tests passed"
3. `pytest tests/test_phase1_infra.py -v` — all tests pass
4. SETUP.md exists, mentions smoke_test.py, and a human confirmed it works
</verification>

<success_criteria>
- SETUP.md exists at project root with four-step onboarding sequence
- Human verified: following SETUP.md and running python3 scripts/smoke_test.py produces "All smoke tests passed"
- All three INFRA requirements verified: INFRA-01 (zero hardcoded paths), INFRA-02 (pip install succeeds), INFRA-03 (setup docs + smoke test pass)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-02-SUMMARY.md` using the summary template.
</output>
