---
phase: 04-evaluation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_compute_ic.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02

must_haves:
  truths:
    - "All metric functions (IC, ICIR, MAE, RMSE, accuracy, F1) have unit tests that run without the real CSVs"
    - "Tests fail (RED) before compute_ic.py exists, confirming stubs catch regressions"
    - "pytest tests/test_compute_ic.py -x -q collects 6 tests and exits with the correct failure/skip state"
  artifacts:
    - path: "tests/test_compute_ic.py"
      provides: "Unit test suite for compute_ic.py functions"
      exports:
        - test_ic_known_correlation
        - test_ic_nan_handling
        - test_icir_formula
        - test_mae_rmse_zero_for_perfect
        - test_f1_perfect_classification
        - test_smoke_actual_output
  key_links:
    - from: "tests/test_compute_ic.py"
      to: "scripts/compute_ic.py"
      via: "import compute_ic_metrics, compute_regression_metrics, compute_classification_metrics"
      pattern: "from scripts\\.compute_ic import"
---

<objective>
Write the Wave 0 test scaffold for the compute_ic.py evaluator. Tests are written before the implementation exists, establishing the behavioral contract that Plan 02 must satisfy.

Purpose: Ensure every metric function has a verifiable correctness check before any implementation is written. This prevents shipping a script that prints numbers but computes them incorrectly.
Output: tests/test_compute_ic.py with 6 passing-or-xfail tests covering EVAL-01 and EVAL-02.
</objective>

<execution_context>
@/Users/gonzaloalonsolidon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gonzaloalonsolidon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-evaluation/04-CONTEXT.md
@.planning/phases/04-evaluation/04-RESEARCH.md
@.planning/phases/04-evaluation/04-VALIDATION.md

<interfaces>
<!-- Existing test infrastructure -->

From tests/conftest.py (already exists with shared fixtures):
- conftest.py is present; no new conftest changes needed for this phase.

From lib/Multitask_Stockformer_utils.py (reference only):
```python
def metric(reg_pred, reg_label, class_pred, class_label):
    # returns (acc, mae, rmse, mape)
    ...
```

Target module (does NOT exist yet — plan 02 creates it):
```python
# scripts/compute_ic.py
def compute_ic_metrics(reg_pred: np.ndarray, reg_label: np.ndarray) -> tuple[float, float, np.ndarray]:
    """Returns (ic_mean, icir, ic_per_day)"""

def compute_regression_metrics(reg_pred: np.ndarray, reg_label: np.ndarray) -> tuple[float, float]:
    """Returns (mae, rmse)"""

def compute_classification_metrics(cls_preds_2d: np.ndarray, cls_label_flat: np.ndarray) -> tuple[float, float]:
    """Returns (acc, f1)"""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test scaffold for compute_ic.py</name>
  <files>tests/test_compute_ic.py</files>
  <behavior>
    - test_ic_known_correlation: compute_ic_metrics(label, label) returns ic_per_day all equal to 1.0 (perfect rank correlation)
    - test_ic_nan_handling: when one day's predictions are constant (zero variance), ic_per_day[that_day] is NaN; ic_mean is not NaN (NaN days excluded)
    - test_icir_formula: ICIR = mean(ic) / std(ic, ddof=1); verified on a 10-day synthetic IC series where mean=0.05, std=0.1 -> icir=0.5
    - test_mae_rmse_zero_for_perfect: compute_regression_metrics(data, data) returns mae=0.0, rmse=0.0
    - test_f1_perfect_classification: compute_classification_metrics(preds_2d, labels_flat) returns acc=1.0, f1=1.0 when predictions match labels exactly
    - test_smoke_actual_output: marked xfail(strict=False) — imports main() and verifies it exits cleanly (or is skipped if output CSVs not present); this is the integration smoke test for Plan 02's deliverable
  </behavior>
  <action>
    Create tests/test_compute_ic.py with the following structure.

    IMPORTANT: tests/conftest.py already exists — do not create a new conftest.py.

    Use `pytest.importorskip` or `pytest.mark.xfail(strict=False)` for any test that depends on scripts/compute_ic.py not yet existing, so the suite collects cleanly.

    The preferred pattern (consistent with project decisions in STATE.md) is `xfail(strict=False)` — the suite collects without failing until implementation lands.

    Include these 6 tests:

    1. test_ic_known_correlation
       - Create n_days=10, n_stocks=50 random label array
       - Call compute_ic_metrics(label, label) — perfect rank correlation
       - Assert np.allclose(ic_per_day, 1.0)

    2. test_ic_nan_handling
       - Create 5x20 random pred/label arrays
       - Set pred[2, :] = 0.5 (constant predictions -> NaN IC on day 2)
       - Assert ic_per_day[2] is NaN
       - Assert ic_mean is not NaN (NaN filtered before mean)

    3. test_icir_formula
       - Construct synthetic ic_per_day of 10 values: fixed sequence with known mean and std
       - Call compute_ic_metrics with synthetic inputs that produce exactly this IC series (or directly call the ICIR formula on a mocked ic_per_day via monkeypatching)
       - Alternative: patch np.array internals is complex; instead construct pred/label arrays where Spearman IC per day is predictable, OR just verify ic_std>0 and icir = ic_mean/ic_std
       - Acceptable simplification: assert icir == pytest.approx(ic_mean / np.std(valid_ic, ddof=1), rel=1e-5) for the returned values

    4. test_mae_rmse_zero_for_perfect
       - data = np.random.randn(20, 50)
       - Assert compute_regression_metrics(data, data) == (0.0, 0.0)

    5. test_f1_perfect_classification
       - labels = np.array([0, 1, 0, 1, 0, 1])
       - preds_2d = labels.reshape(2, 3)
       - Assert compute_classification_metrics(preds_2d, labels) == (1.0, 1.0)

    6. test_smoke_actual_output
       - Use @pytest.mark.xfail(strict=False, reason="Integration test: requires scripts/compute_ic.py and actual inference outputs")
       - Import scripts/compute_ic main() and call with a mock --output_dir arg pointing to output/Multitask_output_SP500_2018-2024
       - If output_dir does not exist, the test is allowed to xfail — the mark covers that case
       - If it does exist and main() runs to completion (exit code 0), the test passes

    Do NOT import from scripts.compute_ic at module level — wrap imports inside each test function so the file is importable even before Plan 02 creates compute_ic.py. Use try/except ImportError -> pytest.skip() inside test bodies.
  </action>
  <verify>
    <automated>cd /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer && python -m pytest tests/test_compute_ic.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    pytest collects 6 tests from tests/test_compute_ic.py. Tests 1-5 are XFAIL or ERROR (not FAILED) because the script does not exist yet. test_smoke_actual_output is XFAIL. The suite does not exit with code 1 due to unexpected failures — all failures are anticipated via xfail(strict=False).
  </done>
</task>

</tasks>

<verification>
pytest tests/test_compute_ic.py -x -q collects 6 tests. No unexpected ERROR or FAILED (only XFAIL/SKIP before implementation lands).
</verification>

<success_criteria>
tests/test_compute_ic.py exists with 6 tests. Running pytest on it does not crash the suite. All tests are protected with xfail(strict=False) so RED phase is tracked but does not block CI.
</success_criteria>

<output>
After completion, create .planning/phases/04-evaluation/04-01-SUMMARY.md
</output>
