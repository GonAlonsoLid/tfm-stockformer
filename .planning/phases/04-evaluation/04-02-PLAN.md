---
phase: 04-evaluation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - scripts/compute_ic.py
autonomous: false
requirements:
  - EVAL-01
  - EVAL-02

must_haves:
  truths:
    - "Running python scripts/compute_ic.py --output_dir <dir> prints a formatted table with IC_mean, ICIR, MAE, RMSE, Accuracy, F1"
    - "evaluation_summary.csv is written to output_dir with exactly one data row and columns: IC_mean, ICIR, IC_pearson, MAE, RMSE, Accuracy, F1_macro"
    - "ic_by_day.csv is written to output_dir with one row per test trading day and columns: day, IC, IC_pearson"
    - "All 6 unit tests in tests/test_compute_ic.py pass (GREEN after RED)"
    - "Script exits 0 on valid inputs and non-zero on missing CSV paths"
  artifacts:
    - path: "scripts/compute_ic.py"
      provides: "Standalone evaluator CLI script"
      exports:
        - compute_ic_metrics
        - compute_regression_metrics
        - compute_classification_metrics
        - main
    - path: "output/Multitask_output_SP500_2018-2024/evaluation_summary.csv"
      provides: "One-row metrics summary for thesis"
      contains: "IC_mean,ICIR,IC_pearson,MAE,RMSE,Accuracy,F1_macro"
    - path: "output/Multitask_output_SP500_2018-2024/ic_by_day.csv"
      provides: "Daily IC time series for thesis plots"
      contains: "day,IC,IC_pearson"
  key_links:
    - from: "scripts/compute_ic.py"
      to: "output/.../regression/regression_pred_last_step.csv"
      via: "pd.read_csv(..., header=None)"
      pattern: "pd\\.read_csv.*header=None"
    - from: "scripts/compute_ic.py"
      to: "output/.../classification/classification_pred_last_step.csv"
      via: "re.findall + np.argmax (parse stringified logit arrays)"
      pattern: "re\\.findall"
    - from: "tests/test_compute_ic.py"
      to: "scripts/compute_ic.py"
      via: "per-test function import"
      pattern: "from scripts\\.compute_ic import"
---

<objective>
Implement scripts/compute_ic.py — the standalone evaluation script that reads inference output CSVs and computes the complete metrics table: IC, ICIR, MAE, RMSE, accuracy, F1. Includes a human-verify checkpoint to confirm console output and output CSVs look correct.

Purpose: Deliver EVAL-01 and EVAL-02 — the primary quantitative evaluation deliverable of the thesis. A single command produces the full results table.
Output: scripts/compute_ic.py + evaluation_summary.csv + ic_by_day.csv written into output_dir.
</objective>

<execution_context>
@/Users/gonzaloalonsolidon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gonzaloalonsolidon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-evaluation/04-CONTEXT.md
@.planning/phases/04-evaluation/04-RESEARCH.md
@.planning/phases/04-evaluation/04-01-SUMMARY.md

<interfaces>
<!-- Locked decisions from CONTEXT.md (non-negotiable) -->

Entry point:
  python scripts/compute_ic.py --output_dir output/Multitask_output_SP500_2018-2024

Input CSVs (paths relative to output_dir):
  regression/regression_pred_last_step.csv     — (167, 478) float matrix, header=None
  regression/regression_label_last_step.csv    — (167, 478) float matrix, header=None
  classification/classification_pred_last_step.csv   — (167, 478) stringified logit arrays e.g. "[ 0.168 -0.090]"
  classification/classification_label_last_step.csv  — (167, 478) binary int labels, header=None

Output files written to output_dir:
  evaluation_summary.csv  — one row: IC_mean, ICIR, IC_pearson, MAE, RMSE, Accuracy, F1_macro
  ic_by_day.csv           — columns: day (0-based int), IC (Spearman), IC_pearson

Key patterns from 04-RESEARCH.md:
  - spearmanr(...).statistic  (NOT .correlation)
  - np.std(ic, ddof=1)  for ICIR denominator
  - re.findall(r"[-+]?\d*\\.?\d+(?:e[-+]?\d+)?", str(cell))  for cls_pred parsing
  - f1_score(labels_flat, preds_flat, average="macro")
  - os.makedirs(output_dir, exist_ok=True)  before writing outputs

Discretion resolutions from 04-RESEARCH.md:
  - Add IC_pearson as bonus column (np.corrcoef per day, row 0-col 1)
  - F1 averaging: macro (classes 47.6%/52.4% nearly balanced; macro is academically conservative)
  - NaN IC: drop from ICIR, log count to stderr, preserve raw NaN in ic_by_day.csv
  - Table formatting: Python f-strings (tabulate not installed)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement scripts/compute_ic.py</name>
  <files>scripts/compute_ic.py</files>
  <behavior>
    - compute_ic_metrics(label, label) returns ic_per_day all 1.0 (test_ic_known_correlation)
    - compute_ic_metrics with one constant-pred day returns NaN for that day, non-NaN ic_mean (test_ic_nan_handling)
    - compute_regression_metrics(data, data) returns (0.0, 0.0) (test_mae_rmse_zero_for_perfect)
    - compute_classification_metrics(perfect_preds_2d, labels) returns (1.0, 1.0) (test_f1_perfect_classification)
    - test_icir_formula: icir = ic_mean / std(valid_ic, ddof=1)
    - test_smoke_actual_output: main() runs to completion on actual output CSVs (if present)
  </behavior>
  <action>
    Implement scripts/compute_ic.py following the complete skeleton in 04-RESEARCH.md, with these additions and constraints:

    **Module structure (in order):**
    1. Module docstring with usage example
    2. Imports: argparse, os, re, sys, numpy, pandas, scipy.stats.spearmanr, sklearn.metrics.f1_score
    3. Function: load_regression(output_dir) -> (reg_pred, reg_label) as np.ndarray float64
    4. Function: load_classification(output_dir) -> (cls_preds_2d, cls_label_flat) — cls_preds_2d is (n_days, n_stocks) int array; cls_label_flat is (n_days*n_stocks,) int array
    5. Function: parse_cls_pred_csv(path) -> np.ndarray — used internally by load_classification; handles stringified logit format
    6. Function: compute_ic_metrics(reg_pred, reg_label) -> (ic_mean: float, icir: float, ic_per_day: np.ndarray)
       - Compute Spearman IC per day: spearmanr(pred[d], label[d]).statistic
       - Also compute Pearson IC per day: np.corrcoef(pred[d], label[d])[0, 1]
       - Return ic_mean (Spearman), icir (Spearman), ic_per_day as structured dict OR return two parallel arrays
       - PREFERRED: return (ic_mean_spearman, icir, ic_per_day_spearman, ic_mean_pearson, ic_per_day_pearson) as 5-tuple so main() can write both to CSVs
       - NaN handling: filter NaN from valid_ic before mean/std; log count to stderr; preserve raw NaN in returned ic_per_day arrays
       - Use ddof=1 for ICIR std
    7. Function: compute_regression_metrics(reg_pred, reg_label) -> (mae: float, rmse: float)
    8. Function: compute_classification_metrics(cls_preds_2d, cls_label_flat) -> (acc: float, f1: float)
       - Flatten cls_preds_2d to 1D before sklearn
       - f1_score(average="macro")
    9. Function: main() — argparse with --output_dir (required), load all 4 CSVs, compute all metrics, print table, save CSVs

    **Console output format (f-strings, no tabulate):**
    ```
    === Evaluation Summary ===
      IC mean (Spearman) : +0.XXXXXX
      IC mean (Pearson)  : +0.XXXXXX
      ICIR               : +0.XXXXXX
      MAE                : 0.XXXXXX
      RMSE               : 0.XXXXXX
      Accuracy           : 0.XXXX
      F1 (macro)         : 0.XXXX
    ```

    **evaluation_summary.csv columns:** IC_mean, ICIR, IC_pearson, MAE, RMSE, Accuracy, F1_macro
    (single data row, index=False)

    **ic_by_day.csv columns:** day (0-based int), IC (Spearman per day), IC_pearson (Pearson per day)
    (index=False; NaN values preserved as empty cells for days with constant predictions)

    **Error handling:**
    - If output_dir does not exist: print error to stderr, sys.exit(1)
    - If any required CSV path is missing: print which file, sys.exit(1)
    - os.makedirs(output_dir, exist_ok=True) before writing output files

    **Compatibility:** imports only numpy, pandas, scipy, sklearn, re, argparse, os, sys — all in requirements.txt. No new dependencies.

    After implementing, run the test suite to confirm tests turn GREEN:
    pytest tests/test_compute_ic.py -x -q
  </action>
  <verify>
    <automated>cd /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer && python -m pytest tests/test_compute_ic.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 6 tests in tests/test_compute_ic.py pass (GREEN). scripts/compute_ic.py is importable with no errors. test_ic_known_correlation, test_ic_nan_handling, test_icir_formula, test_mae_rmse_zero_for_perfect, test_f1_perfect_classification all PASS. test_smoke_actual_output PASSES or XPASS if actual output CSVs are present.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — confirm metrics output and CSV files</name>
  <what-built>
    scripts/compute_ic.py — standalone evaluation script that reads the four inference output CSVs and computes IC, ICIR, MAE, RMSE, accuracy, F1 (macro). Also writes evaluation_summary.csv and ic_by_day.csv into the output directory.

    All 6 unit tests are passing (GREEN from Task 1).
  </what-built>
  <how-to-verify>
    1. Confirm the unit tests are green:
       cd /path/to/tfm-stockformer
       pytest tests/test_compute_ic.py -x -q
       Expected: 6 passed (or 5 passed + 1 xpass if output CSVs are present)

    2. If you have the inference output CSVs available (output/Multitask_output_SP500_2018-2024/regression/ and classification/), run the script:
       python scripts/compute_ic.py --output_dir output/Multitask_output_SP500_2018-2024
       Expected: Console table printed with IC_mean, ICIR, MAE, RMSE, Accuracy, F1 values. No errors.

    3. Check that evaluation_summary.csv was written:
       cat output/Multitask_output_SP500_2018-2024/evaluation_summary.csv
       Expected: header row + 1 data row with all metric values

    4. Check that ic_by_day.csv was written:
       head output/Multitask_output_SP500_2018-2024/ic_by_day.csv
       Expected: header (day,IC,IC_pearson) + rows for each test day

    5. Verify console table formatting looks readable for thesis use.

    NOTE: If output CSVs from inference do not exist on this machine yet (pipeline not run), steps 2-5 can be skipped — unit tests (step 1) are the primary gate. Run inference first if you want end-to-end validation:
       python scripts/run_inference.py --config config/Multitask_Stock_SP500.conf
  </how-to-verify>
  <resume-signal>Type "approved" if the output looks correct, or describe any issues found.</resume-signal>
  <files>output/Multitask_output_SP500_2018-2024/evaluation_summary.csv, output/Multitask_output_SP500_2018-2024/ic_by_day.csv</files>
  <action>Human verifies console output formatting and CSV contents after running python scripts/compute_ic.py --output_dir output/Multitask_output_SP500_2018-2024</action>
  <verify>
    <automated>python -m pytest tests/test_compute_ic.py -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>Console output is readable, evaluation_summary.csv has 1 data row with all metric columns, ic_by_day.csv has one row per test day. Human types "approved".</done>
</task>

</tasks>

<verification>
pytest tests/test_compute_ic.py -x -q returns 6 passed.
python scripts/compute_ic.py --output_dir output/Multitask_output_SP500_2018-2024 prints a complete metrics table and writes evaluation_summary.csv and ic_by_day.csv.
</verification>

<success_criteria>
1. scripts/compute_ic.py exists and is importable
2. pytest tests/test_compute_ic.py -x -q: 6 tests pass (GREEN)
3. On actual output CSVs: evaluation_summary.csv has exactly 1 data row with IC_mean, ICIR, IC_pearson, MAE, RMSE, Accuracy, F1_macro
4. ic_by_day.csv has one row per test day with Spearman and Pearson IC
5. Human confirms console output is readable and values are in plausible ranges
</success_criteria>

<output>
After completion, create .planning/phases/04-evaluation/04-02-SUMMARY.md
</output>
