---
phase: 05-portfolio-backtesting
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_backtest.py
autonomous: true
requirements:
  - PORT-01
  - PORT-02
  - PORT-03
  - BACK-01
  - BACK-02
  - BACK-03

must_haves:
  truths:
    - "pytest collects tests/test_backtest.py without import errors"
    - "Six xfail stubs exist — one per requirement: test_top_k_selection, test_equal_weight, test_transaction_cost, test_cumulative_return, test_performance_metrics, test_alpha_beta"
    - "All stubs xfail (not error) so the suite is GREEN before implementation"
  artifacts:
    - path: "tests/test_backtest.py"
      provides: "xfail test stubs for all Phase 5 requirements"
      exports: ["test_top_k_selection", "test_equal_weight", "test_transaction_cost", "test_cumulative_return", "test_performance_metrics", "test_alpha_beta"]
  key_links:
    - from: "tests/test_backtest.py"
      to: "scripts/run_backtest.py"
      via: "imports inside test body (not module level) to avoid ImportError before implementation"
      pattern: "pytest.importorskip|import inside def"
---

<objective>
Create the Wave 0 test scaffold for Phase 5: six xfail stubs in tests/test_backtest.py, one per requirement.

Purpose: Establish the Nyquist-compliant test contract before any implementation lands. Stubs must collect and xfail — not error — so the CI suite stays GREEN while Plans 02 and 03 implement the production code.
Output: tests/test_backtest.py with six named stubs.
</objective>

<execution_context>
@/Users/gonzaloalonsolidon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gonzaloalonsolidon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/compute_ic.py
@tests/conftest.py
</context>

<interfaces>
<!-- Project xfail stub pattern (established Phase 02-01 and 04-01) -->
<!-- Imports must be inside test body to avoid module-level ImportError -->

From tests/conftest.py:
```python
@pytest.fixture
def sp500_ohlcv_fixture():
    # dict: ticker -> DataFrame(Open/High/Low/Close/Volume), 300 bdays

@pytest.fixture
def feature_matrix_fixture():
    # np.ndarray shape [280, 5], float32

@pytest.fixture
def project_root():
    # str — absolute path to repo root
```

Phase 4 stub pattern (tests/test_compute_ic.py, Plan 04-01):
```python
import pytest

@pytest.mark.xfail(strict=False, reason="Wave 0 stub — not yet implemented")
def test_something():
    import scripts.compute_ic as m  # import inside body
    pytest.fail("stub")
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_backtest.py with six xfail stubs</name>
  <files>tests/test_backtest.py</files>
  <action>
Create tests/test_backtest.py following the established Phase 2/4 xfail pattern (strict=False, import inside body):

```
PORT-01 → test_top_k_selection
PORT-02 → test_equal_weight
PORT-03 → test_transaction_cost
BACK-01 → test_cumulative_return
BACK-02 → test_performance_metrics
BACK-03 → test_alpha_beta
```

Each stub:
1. Decorated with `@pytest.mark.xfail(strict=False, reason="Wave 0 stub — not yet implemented")`
2. Imports from `scripts.run_backtest` INSIDE the test body (not at module level) — prevents ImportError before the module exists
3. Calls `pytest.fail("stub")` as the test body so it xfails immediately

Include a module-level docstring explaining the test contract and the mapping to requirement IDs.

Do NOT import `scripts.run_backtest` at the top of the file. Do NOT use `strict=True` (suite must stay GREEN).
  </action>
  <verify>
    <automated>cd /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer && python -m pytest tests/test_backtest.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>python -m pytest tests/test_backtest.py -x -q exits 0, reports 6 xfailed, 0 errors, 0 failed</done>
</task>

</tasks>

<verification>
python -m pytest tests/test_backtest.py -x -q must exit 0 with 6 xfailed.
python -m pytest tests/ -q must exit 0 (full suite clean).
</verification>

<success_criteria>
- tests/test_backtest.py exists with 6 named stubs
- All 6 stubs xfail (not error) under strict=False
- Full test suite (tests/) remains green
- No module-level imports of scripts.run_backtest
</success_criteria>

<output>
After completion, create `.planning/phases/05-portfolio-backtesting/05-01-SUMMARY.md`
</output>
