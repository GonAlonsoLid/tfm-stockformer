---
phase: 09-pipeline-cleanup-and-restructuring
plan: 04
type: execute
wave: 3
depends_on: [09-03]
files_modified:
  - README.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "data_processing_script/ directory no longer exists in the repository"
    - "Full test suite passes after deletion"
    - "README.md contains a Quick Start section with all 7 reproduction commands"
    - "README.md has no references to data_processing_script/"
    - "Human reviewer confirms README commands are accurate and the GPU note is present"
  artifacts:
    - path: "README.md"
      provides: "Quick Start / Pipeline Reproduction section with 7 commands"
      contains: "## Quick Start"
  key_links:
    - from: "README.md Quick Start"
      to: "scripts/build_pipeline.py"
      via: "Command 2: python scripts/build_pipeline.py --config config/Multitask_Stock_SP500.conf"
      pattern: "build_pipeline\\.py --config"
---

<objective>
Delete the legacy `data_processing_script/` directory tree and rewrite the README with a Quick Start section listing the full reproduction command sequence. After this plan the repository contains no Chinese-market legacy code and any reader can reproduce the S&P500 pipeline from a fresh clone by following README alone.

Purpose: Finalize the cleanup — remove dead code and provide accurate end-to-end documentation.
Output: `data_processing_script/` deleted; `README.md` with Quick Start section; human checkpoint confirming accuracy.
</objective>

<execution_context>
@/Users/gonzaloalonsolidon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gonzaloalonsolidon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-pipeline-cleanup-and-restructuring/09-CONTEXT.md
@.planning/phases/09-pipeline-cleanup-and-restructuring/09-03-SUMMARY.md

<interfaces>
<!-- README.md current structure (lines 1-112): -->
<!-- - "Stockformer Data File Explanation..." title -->
<!-- - "Stockformer Code Overview" section -->
<!-- - Dataset table (14 subsets) -->
<!-- - "## File Description" section (lines 38-96) — MUST BE REPLACED -->
<!--   References: data_processing_script/, stockformer_input_data_processing/, volume_and_price_factor_construction/ -->
<!-- - "## How to Run" section (lines 98-103) — references old python Multitask_Stockformer_models.py -->
<!-- - "## Citation" section (lines 105-112) — keep unchanged -->

<!-- The 7 reproduction commands per CONTEXT.md (locked): -->
# 1. pip install -r requirements.txt
# 2. python scripts/build_pipeline.py --config config/Multitask_Stock_SP500.conf
# 3. python MultiTask_Stockformer_train.py --config config/Multitask_Stock_SP500.conf
# 4. python scripts/run_inference.py --config config/Multitask_Stock_SP500.conf
# 5. python scripts/compute_ic.py
# 6. python scripts/run_backtest.py
# 7. streamlit run app.py

<!-- GPU note required (locked): step 3 requires GPU; Kaggle recommended; all others run on CPU -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete data_processing_script/ tree and verify test suite</name>
  <files>
    data_processing_script/ (deleted — all subdirectories and files)
  </files>
  <read_first>
    - scripts/build_pipeline.py (confirm PIPELINE_DIR no longer references data_processing_script/ — sanity check before deletion)
    - tests/test_phase1_infra.py (scan for any reference to data_processing_script/ that would break after deletion)
    - tests/conftest.py (scan for any reference to data_processing_script/)
  </read_first>
  <action>
Delete the entire `data_processing_script/` directory tree including all subdirectories and files.

The directories being deleted are (per locked decision):
- `data_processing_script/volume_and_price_factor_construction/` — Qlib TA Jupyter notebooks, obsolete
- `data_processing_script/stockformer_input_data_processing/` — Chinese-market scripts (`data_Interception.py`, `Stockformer_data_preprocessing_script.py`, `results_data_processing.py`), superseded
- `data_processing_script/sp500_pipeline/` — already copied to `scripts/sp500_pipeline/` in Plan 09-02; safe to delete now
- `data_processing_script/` root — now empty after the above are removed

Use `shutil.rmtree("data_processing_script")` from the project root, or equivalent shell command. The directory to delete is `data_processing_script/` relative to the project root.

Safety precondition: Verify that `scripts/build_pipeline.py` PIPELINE_DIR does NOT contain `data_processing_script` before running the delete. If it does, STOP and report — it means Plan 09-02 did not complete correctly.

Verification command after deletion: `python -m pytest tests/ -x -q` must exit 0.
  </action>
  <verify>
    <automated>cd /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer && python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls data_processing_script/ 2>&1` contains "No such file or directory" (directory does not exist)
    - `grep -r "data_processing_script" scripts/build_pipeline.py` returns no matches
    - `python -m pytest tests/ -x -q` exits 0 (full suite green after deletion)
  </acceptance_criteria>
  <done>data_processing_script/ is gone; full test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md with Quick Start section</name>
  <files>README.md</files>
  <read_first>
    - README.md (FULL FILE — must read current state before overwriting any section)
  </read_first>
  <action>
Rewrite README.md. Keep the following sections unchanged:
1. The title line ("# Stockformer Data File Explanation...")
2. The "[中文版本]" link on line 3
3. The "## Stockformer Code Overview" section including the paper links
4. The dataset table (14 subsets)
5. The "## Citation" section (at the end)

Replace "## File Description" (the section from line 38 to approximately line 96) and "## How to Run" (approximately lines 98-103) with the following content:

---
## File Structure

```
scripts/
├── build_pipeline.py        # Data pipeline orchestrator (steps 1–5)
├── build_alpha360.py        # Alpha360 feature builder (invoked by build_pipeline.py)
├── sp500_pipeline/          # Step scripts: download, normalize, serialize, graph embedding
├── run_inference.py         # Standalone inference on saved checkpoint
├── compute_ic.py            # IC / ICIR evaluation
└── run_backtest.py          # Portfolio backtest vs SPY

config/
└── Multitask_Stock_SP500.conf   # All paths and hyperparameters for the S&P500 run

data/
└── Stock_SP500_2018-01-01_2024-01-01/   # Created by build_pipeline.py
    ├── ohlcv/               # Parquet files (one per ticker)
    ├── features/            # 360 Alpha360-style price/volume ratio CSVs
    ├── flow.npz             # Normalized return sequences
    ├── trend_indicator.npz  # Trend label arrays
    └── 128_corr_struc2vec_adjgat.npy  # Graph embeddings

app.py                       # Streamlit interface
MultiTask_Stockformer_train.py  # Model training entry point
```

## Quick Start

Reproduce the full pipeline from a fresh clone in seven steps.

### 1. Install dependencies

```sh
pip install -r requirements.txt
```

### 2. Build data pipeline (download → features → embeddings)

```sh
python scripts/build_pipeline.py --config config/Multitask_Stock_SP500.conf
```

This single command runs all five steps:
- Downloads S&P 500 OHLCV data via yfinance
- Normalizes and splits the dataset
- Serializes `flow.npz` and `trend_indicator.npz`
- Builds Struc2Vec graph embeddings
- Generates 360 Alpha360-style price/volume ratio features

On re-runs, completed steps are skipped automatically (sentinel-based idempotency).

### 3. Train the model

```sh
python MultiTask_Stockformer_train.py --config config/Multitask_Stock_SP500.conf
```

> **GPU required for reasonable runtime.** The training script runs on CPU but will be extremely slow. [Kaggle](https://www.kaggle.com/) provides free GPU notebooks suitable for this workload. For a quick smoke test (2 epochs), add `--max_epoch 2`.

### 4. Run inference

```sh
python scripts/run_inference.py --config config/Multitask_Stock_SP500.conf
```

Generates prediction CSVs in `output/` using the saved checkpoint.

### 5. Compute IC / ICIR evaluation metrics

```sh
python scripts/compute_ic.py
```

Prints Spearman IC, ICIR, and Pearson IC for the test period.

### 6. Run portfolio backtest

```sh
python scripts/run_backtest.py
```

Builds a daily-rebalanced top-K portfolio, computes returns vs SPY, and saves `backtest_results.csv` and `backtest_positions.csv`.

### 7. Launch the Streamlit interface

```sh
streamlit run app.py
```

Opens an interactive browser interface for exploring predictions and backtest results.

---
  </action>
  <verify>
    <automated>cd /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer && grep -c "data_processing_script" README.md && echo "above must be 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "data_processing_script" README.md` returns no matches (exit code 1, no output)
    - `grep "## Quick Start" README.md` returns a match
    - `grep "build_pipeline.py --config" README.md` returns a match
    - `grep "MultiTask_Stockformer_train.py" README.md` returns a match
    - `grep "GPU required" README.md` returns a match
    - `grep "streamlit run app.py" README.md` returns a match
    - `grep "## Citation" README.md` returns a match (citation section preserved)
  </acceptance_criteria>
  <done>README.md contains Quick Start with all 7 commands; no references to data_processing_script/; Citation section intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify README accuracy and legacy deletion</name>
  <what-built>
    - data_processing_script/ deleted (all legacy Chinese-market code gone)
    - README.md rewritten with ## Quick Start section listing 7 reproduction commands
    - All tests passing after deletion
  </what-built>
  <how-to-verify>
    1. Open README.md and read the "## Quick Start" section
    2. Verify all 7 steps are present in the correct order:
       - Step 1: pip install -r requirements.txt
       - Step 2: python scripts/build_pipeline.py --config config/Multitask_Stock_SP500.conf
       - Step 3: python MultiTask_Stockformer_train.py --config config/Multitask_Stock_SP500.conf
       - Step 4: python scripts/run_inference.py --config config/Multitask_Stock_SP500.conf
       - Step 5: python scripts/compute_ic.py
       - Step 6: python scripts/run_backtest.py
       - Step 7: streamlit run app.py
    3. Confirm the GPU note is present for step 3 (training requires GPU; Kaggle recommended)
    4. Confirm there are no references to data_processing_script/ anywhere in README.md
    5. Run: python scripts/build_pipeline.py --help
       Confirm output lists: --config, --data_dir, --start, --end, --force
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 9, or describe any issues for correction</resume-signal>
  <action>Human reviews README.md Quick Start section and confirms --help output matches expected flags.</action>
  <verify>Human confirms all 7 commands present, GPU note visible, no data_processing_script/ references.</verify>
  <done>User types "approved" after confirming README accuracy and --help output.</done>
</task>

</tasks>

<verification>
After all tasks complete:
- `ls data_processing_script/ 2>&1` shows "No such file or directory"
- `grep -r "data_processing_script" README.md scripts/` returns no matches
- `python -m pytest tests/ -x -q` exits 0
- README.md Quick Start section is human-verified as accurate
</verification>

<success_criteria>
- data_processing_script/ does not exist
- README.md has ## Quick Start with all 7 commands and GPU note
- No references to data_processing_script/ remain in README.md or any active script
- Full test suite green
- Human approved README accuracy
</success_criteria>

<output>
After completion, create `.planning/phases/09-pipeline-cleanup-and-restructuring/09-04-SUMMARY.md`
</output>
