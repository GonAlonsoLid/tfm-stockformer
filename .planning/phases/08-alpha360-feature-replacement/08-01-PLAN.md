---
phase: 08-alpha360-feature-replacement
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_build_alpha360.py
autonomous: true
requirements:
  - ALPHA360-01
  - ALPHA360-02
  - ALPHA360-03
  - ALPHA360-04
  - ALPHA360-05

must_haves:
  truths:
    - "pytest tests/test_build_alpha360.py -x exits with all tests collected (xfail or pass)"
    - "All 5 test function names covering ALPHA360-01..05 exist in the file"
    - "Tests use tmp_path fixtures with synthetic Parquet data — no real data dependency"
    - "No import of build_alpha360 at module level (imports inside test bodies to avoid ImportError)"
  artifacts:
    - path: "tests/test_build_alpha360.py"
      provides: "xfail stubs for ALPHA360-01 through ALPHA360-05"
      min_lines: 80
  key_links:
    - from: "tests/test_build_alpha360.py"
      to: "scripts/build_alpha360.py"
      via: "import inside test body (guarded by xfail)"
      pattern: "import build_alpha360\\|from scripts"
---

<objective>
Create the Wave 0 test scaffold for Phase 8: a pytest file with 5 xfail stubs covering all
ALPHA360 requirements. Tests use synthetic Parquet fixtures in tmp_path so they run offline
without the full 478-ticker dataset.

Purpose: Establish the test contract before implementation. Green suite (all xfail) gates
Wave 1 implementation.
Output: tests/test_build_alpha360.py with 5 named tests, all initially xfail.
</objective>

<execution_context>
@/Users/gonzaloalonsolidon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gonzaloalonsolidon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-alpha360-feature-replacement/08-CONTEXT.md
@.planning/phases/08-alpha360-feature-replacement/08-RESEARCH.md

<interfaces>
<!-- Project test conventions — read from tests/test_data_pipeline.py and tests/test_compute_ic.py -->

Key patterns:
1. xfail stubs use: @pytest.mark.xfail(strict=False, reason="Wave 0 stub — implementation pending")
2. Imports inside test bodies to prevent module-level ImportError (project-wide convention per STATE.md decisions)
3. tmp_path fixtures for synthetic data (pytest built-in)
4. Parquet file format from live data: columns = ['Open', 'High', 'Low', 'Close', 'Volume'], index = DatetimeIndex

From config/Multitask_Stock_SP500.conf:
  alpha_360_dir = ./data/Stock_SP500_2018-01-01_2024-01-01/features
  (ohlcv_dir derived as parent/ohlcv/)

From lib/Multitask_Stockformer_utils.py line 147:
  self.infea = bonus_all.shape[-1] + 2   # 360 CSVs -> infea = 362

Verified data contracts:
  OHLCV Parquet columns: ['Open', 'High', 'Low', 'Close', 'Volume']
  Existing feature CSV shape: (1449, 478), first date 2018-03-29
  Ticker count: 478, lag buffer = 60 rows
  OHLCV date range: 2018-01-02 to 2023-12-29 (1509 rows)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write test scaffold with 5 xfail stubs covering ALPHA360-01..05</name>
  <files>tests/test_build_alpha360.py</files>

  <read_first>
    - tests/test_data_pipeline.py (xfail pattern, fixture style, import-inside-body convention)
    - tests/test_compute_ic.py (xfail pattern for recently added tests)
    - .planning/phases/08-alpha360-feature-replacement/08-CONTEXT.md (locked decisions)
    - .planning/phases/08-alpha360-feature-replacement/08-RESEARCH.md (validation snippet, test map)
  </read_first>

  <action>
Create tests/test_build_alpha360.py with the following structure:

MODULE DOCSTRING:
"""Phase 8 Alpha360 feature replacement tests — ALPHA360-01 through ALPHA360-05."""

IMPORTS (top-level, safe):
  import pytest
  import os
  import numpy as np
  import pandas as pd

FIXTURE: `alpha360_env` (scope="function")
  Uses tmp_path. Creates a minimal environment that mirrors the real data layout:

  Structure to create:
    tmp_path/
      config/Multitask_Stock_SP500.conf   <- minimal .conf with [file] alpha_360_dir pointing to tmp_path/features
      data/ohlcv/                          <- synthetic Parquet files
      data/features/                       <- initially populated with 3 dummy CSVs (simulates existing 69)
      data/tickers.txt                     <- 5 tickers: AAPL, MSFT, GOOG, AMZN, META

  Parquet fixture data: create 5 tickers, each a DataFrame with:
    - columns: ['Open', 'High', 'Low', 'Close', 'Volume']
    - index: pd.bdate_range(start='2018-01-02', periods=80)  (80 rows: 60 lag buffer + 20 output rows)
    - values: synthetic but realistic (Close around 100.0, Volume around 1_000_000, no zeros)
    - Use np.random.seed(42) for reproducibility
    Save each as: tmp_path / "data/ohlcv/{ticker}.parquet"

  tickers.txt: write one ticker per line in order: AAPL, MSFT, GOOG, AMZN, META

  Existing dummy features: create 3 CSVs in data/features/ named dummy_01.csv, dummy_02.csv,
  dummy_03.csv. Each is a DataFrame with 20 rows (dates matching Parquet rows[60:]) and 5 cols.
  Purpose: simulate the 69-CSV state to be backed up.

  Config: write minimal .conf file:
    [file]
    alpha_360_dir = {str(tmp_path / "data/features")}

  Fixture returns: dict with keys:
    "tmp_path": tmp_path
    "ohlcv_dir": tmp_path / "data/ohlcv"
    "features_dir": tmp_path / "data/features"
    "tickers_file": tmp_path / "data/tickers.txt"
    "config_path": tmp_path / "config/Multitask_Stock_SP500.conf"
    "tickers": ["AAPL", "MSFT", "GOOG", "AMZN", "META"]

TESTS (all decorated with @pytest.mark.xfail(strict=False, reason="Wave 0 stub — scripts/build_alpha360.py not yet implemented")):

1. test_build_alpha360_creates_360_csvs(alpha360_env)
   ALPHA360-01: verifies the script runs and produces exactly 360 CSV files
   Body:
     import sys, pathlib
     sys.path.insert(0, str(pathlib.Path(__file__).parent.parent / "scripts"))
     import build_alpha360
     build_alpha360.main(config_path=str(alpha360_env["config_path"]),
                          data_dir=str(alpha360_env["tmp_path"] / "data"))
     csv_files = [f for f in os.listdir(alpha360_env["features_dir"]) if f.endswith(".csv")]
     assert len(csv_files) == 360

   NOTE: The fixture has 5 tickers and 80 OHLCV rows (80-60=20 output rows). The script
   must accept a data_dir override for testability. If build_alpha360.main() does not accept
   data_dir, use monkeypatching via config to point ohlcv_dir — see action note below.

2. test_feature_csv_shape_and_no_nan(alpha360_env)
   ALPHA360-02: verifies each CSV has correct shape (T_features, N_tickers), zero NaN, zero inf
   Body:
     [same import as above]
     build_alpha360.main(config_path=str(alpha360_env["config_path"]),
                          data_dir=str(alpha360_env["tmp_path"] / "data"))
     features_dir = alpha360_env["features_dir"]
     csv_files = [f for f in os.listdir(features_dir) if f.endswith(".csv")]
     df = pd.read_csv(os.path.join(features_dir, csv_files[0]), index_col=0)
     assert df.shape[1] == 5  # 5 tickers in fixture
     assert df.isna().sum().sum() == 0, "NaN values found in feature CSV"
     assert not np.isinf(df.values).any(), "Inf values found in feature CSV"

3. test_first_row_date(alpha360_env)
   ALPHA360-03: verifies first date row is index[60] of the OHLCV date range (60-row lag buffer)
   Body:
     [same import as above]
     build_alpha360.main(config_path=str(alpha360_env["config_path"]),
                          data_dir=str(alpha360_env["tmp_path"] / "data"))
     features_dir = alpha360_env["features_dir"]
     csv_files = [f for f in os.listdir(features_dir) if f.endswith(".csv")]
     df = pd.read_csv(os.path.join(features_dir, csv_files[0]), index_col=0)
     # In fixture: bdate_range(start='2018-01-02', periods=80); index[60] is the 61st business day
     ohlcv_dates = pd.bdate_range(start='2018-01-02', periods=80)
     expected_first_date = str(ohlcv_dates[60].date())
     assert str(df.index[0]) == expected_first_date, (
         f"Expected first date {expected_first_date}, got {df.index[0]}"
     )

4. test_backup_created(alpha360_env)
   ALPHA360-04: verifies that a backup directory is created containing the original 3 dummy CSVs
   Body:
     [same import as above]
     build_alpha360.main(config_path=str(alpha360_env["config_path"]),
                          data_dir=str(alpha360_env["tmp_path"] / "data"))
     data_dir = alpha360_env["tmp_path"] / "data"
     backup_dirs = [d for d in os.listdir(data_dir)
                    if d.startswith("features_backup") and os.path.isdir(data_dir / d)]
     assert len(backup_dirs) >= 1, "No backup directory created"
     backup_path = data_dir / backup_dirs[0]
     backup_csvs = [f for f in os.listdir(backup_path) if f.endswith(".csv")]
     assert len(backup_csvs) == 3, f"Expected 3 original CSVs in backup, got {len(backup_csvs)}"

5. test_column_order_matches_tickers(alpha360_env)
   ALPHA360-05: verifies column order in every CSV matches tickers.txt
   Body:
     [same import as above]
     build_alpha360.main(config_path=str(alpha360_env["config_path"]),
                          data_dir=str(alpha360_env["tmp_path"] / "data"))
     features_dir = alpha360_env["features_dir"]
     with open(alpha360_env["tickers_file"]) as f:
         expected_tickers = [line.strip() for line in f if line.strip()]
     csv_files = [f for f in os.listdir(features_dir) if f.endswith(".csv")]
     for csv_file in csv_files[:3]:  # spot-check first 3
         df = pd.read_csv(os.path.join(features_dir, csv_file), index_col=0)
         assert list(df.columns) == expected_tickers, (
             f"{csv_file}: column order {list(df.columns)} != tickers.txt {expected_tickers}"
         )

IMPORTANT DESIGN NOTE for build_alpha360.main() interface:
The test calls build_alpha360.main(config_path=..., data_dir=...).
The data_dir parameter overrides the base data directory, letting the script derive:
  ohlcv_dir  = data_dir / "ohlcv"
  features_dir = from config alpha_360_dir (which already points to data_dir/features in fixture config)
  tickers_file = data_dir / "tickers.txt"
This interface must be designed in the script (Plan 02). The test assumes this signature —
document it prominently in the test file so Plan 02 executor sees it immediately.
  </action>

  <verify>
    <automated>cd /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer && python -m pytest tests/test_build_alpha360.py -x --co -q 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_build_alpha360.py exists
    - grep -c "def test_" tests/test_build_alpha360.py returns 5
    - grep "test_build_alpha360_creates_360_csvs" tests/test_build_alpha360.py exits 0
    - grep "test_feature_csv_shape_and_no_nan" tests/test_build_alpha360.py exits 0
    - grep "test_first_row_date" tests/test_build_alpha360.py exits 0
    - grep "test_backup_created" tests/test_build_alpha360.py exits 0
    - grep "test_column_order_matches_tickers" tests/test_build_alpha360.py exits 0
    - grep "xfail" tests/test_build_alpha360.py exits 0
    - grep "strict=False" tests/test_build_alpha360.py exits 0
    - python -m pytest tests/test_build_alpha360.py -x --co -q exits 0 (collection succeeds with no errors)
    - pytest tests/test_build_alpha360.py -x exits 0 (all xfail = suite GREEN)
  </acceptance_criteria>

  <done>
    5 test functions exist, all xfail(strict=False), pytest collects them without error, full suite remains GREEN (xfail counts as passing).
  </done>
</task>

</tasks>

<verification>
pytest tests/test_build_alpha360.py -x returns 0 (green — all xfail)
pytest tests/ -x returns 0 (no regression in existing suite)
grep -c "def test_" tests/test_build_alpha360.py outputs 5
</verification>

<success_criteria>
Wave 0 complete: tests/test_build_alpha360.py exists with 5 xfail stubs covering ALPHA360-01 through ALPHA360-05, pytest collects and runs them green (all xfail), no regression in existing test suite.
</success_criteria>

<output>
After completion, create .planning/phases/08-alpha360-feature-replacement/08-01-SUMMARY.md
</output>
