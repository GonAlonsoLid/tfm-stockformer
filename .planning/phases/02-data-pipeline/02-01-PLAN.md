---
phase: 02-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_data_pipeline.py
  - tests/conftest.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]

must_haves:
  truths:
    - "pytest tests/test_data_pipeline.py -x -q runs without collection errors"
    - "All test stubs are importable and each maps to a specific requirement"
    - "conftest.py provides synthetic OHLCV fixtures that downstream tests can reuse"
  artifacts:
    - path: "tests/test_data_pipeline.py"
      provides: "Test stubs for DATA-01 through DATA-05"
      min_lines: 80
    - path: "tests/conftest.py"
      provides: "Phase 2 fixtures: synthetic OHLCV DataFrame, synthetic feature matrix"
      contains: "sp500_ohlcv_fixture"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/test_data_pipeline.py"
      via: "pytest fixture injection"
      pattern: "def sp500_ohlcv_fixture"
---

<objective>
Create the Wave 0 test scaffold for Phase 2 so every subsequent task has an automated verify command ready on first commit.

Purpose: The VALIDATION.md requires tests/test_data_pipeline.py to exist before implementation tasks run. Without stubs the Nyquist rule is violated — tasks in plans 02-05 would have no automated verify targets.
Output: tests/test_data_pipeline.py with xfail stubs for each requirement, tests/conftest.py extended with synthetic S&P500 fixtures.
</objective>

<execution_context>
@/Users/gonzaloalonsolidon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gonzaloalonsolidon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-pipeline/02-VALIDATION.md
@.planning/phases/02-data-pipeline/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend conftest.py with Phase 2 fixtures</name>
  <files>tests/conftest.py</files>
  <action>
Append Phase 2 fixtures to tests/conftest.py (do NOT remove existing Phase 1 content).

Add the following fixtures:

1. `sp500_ohlcv_fixture` — returns a dict mapping 5 fake tickers ("AAPL", "MSFT", "GOOG", "AMZN", "META") to DataFrames with columns [Open, High, Low, Close, Volume] and a DatetimeIndex of 300 daily business days starting 2020-01-02. Use `np.random.seed(42)` for reproducibility. Close prices start at 100.0 and apply cumulative random walk. Volume is random integers 1e6–5e6.

2. `ohlcv_wide_fixture` — returns a single wide DataFrame where index=DatetimeIndex (300 days), columns=5 tickers, values=Close prices from sp500_ohlcv_fixture. This is the pivoted form consumed by feature engineering and normalization steps.

3. `feature_matrix_fixture` — returns a numpy ndarray of shape [280, 5] (300 days minus warmup from longest indicator window) with random float values representing a normalized feature matrix. Use `np.random.seed(99)`.

Import numpy and pandas at top if not already imported. Keep existing Phase 1 imports/fixtures intact.
  </action>
  <verify>
    <automated>cd /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer && python -c "import tests.conftest; print('conftest importable')"</automated>
  </verify>
  <done>conftest.py imports without error and defines sp500_ohlcv_fixture, ohlcv_wide_fixture, feature_matrix_fixture</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create test_data_pipeline.py with xfail stubs</name>
  <files>tests/test_data_pipeline.py</files>
  <action>
Create tests/test_data_pipeline.py with xfail stubs for every requirement. Each stub uses `pytest.mark.xfail(reason="...", strict=False)` so the suite collects and reports but does not fail until implementation is committed.

File structure:

```python
"""Phase 2 data pipeline tests — DATA-01 through DATA-05."""
import pytest
import numpy as np
import pandas as pd


# ── DATA-01: OHLCV download ─────────────────────────────────────────────────

@pytest.mark.xfail(reason="DATA-01 not yet implemented", strict=False)
def test_download_parquet_schema(sp500_ohlcv_fixture):
    """Downloaded Parquet files have OHLCV columns and no fully-empty rows."""
    # Will import and call 01_download_ohlcv.download_ohlcv_batched in real test
    pytest.xfail("DATA-01 stub")


@pytest.mark.xfail(reason="DATA-01 not yet implemented", strict=False)
def test_clean_no_all_nan_rows(sp500_ohlcv_fixture):
    """After cleaning, no stock has >5% missing trading days."""
    pytest.xfail("DATA-01 stub")


# ── DATA-02: Feature engineering ────────────────────────────────────────────

@pytest.mark.xfail(reason="DATA-02 not yet implemented", strict=False)
def test_feature_columns_present(ohlcv_wide_fixture):
    """Feature matrix contains RSI, MACD, BB, ROC, VOL_ratio for windows 5/10/20/60."""
    pytest.xfail("DATA-02 stub")


@pytest.mark.xfail(reason="DATA-02 not yet implemented", strict=False)
def test_feature_no_all_nan_columns(ohlcv_wide_fixture):
    """No feature column is entirely NaN after warmup period is dropped."""
    pytest.xfail("DATA-02 stub")


# ── DATA-03: Cross-sectional normalization ───────────────────────────────────

@pytest.mark.xfail(reason="DATA-03 not yet implemented", strict=False)
def test_cross_sectional_normalization(feature_matrix_fixture):
    """Per-date row has mean≈0 and std≈1 across stocks after normalization."""
    pytest.xfail("DATA-03 stub")


# ── DATA-04: Train/val/test split no leakage ─────────────────────────────────

@pytest.mark.xfail(reason="DATA-04 not yet implemented", strict=False)
def test_no_normalization_leakage(feature_matrix_fixture):
    """Val/test normalization does not use statistics from val/test rows."""
    pytest.xfail("DATA-04 stub")


@pytest.mark.xfail(reason="DATA-04 not yet implemented", strict=False)
def test_split_ratios(feature_matrix_fixture):
    """Train is 75%, val is 12.5%, test is 12.5% of total time steps."""
    pytest.xfail("DATA-04 stub")


# ── DATA-05: NPZ arrays + graph embedding ────────────────────────────────────

@pytest.mark.xfail(reason="DATA-05 not yet implemented", strict=False)
def test_npz_shapes_no_nan(tmp_path):
    """flow.npz and trend_indicator.npz have shape [T, N] and no NaN values."""
    pytest.xfail("DATA-05 stub")


@pytest.mark.xfail(reason="DATA-05 not yet implemented", strict=False)
def test_trend_indicator_binary(tmp_path):
    """trend_indicator.npz contains only 0 and 1 values."""
    pytest.xfail("DATA-05 stub")


@pytest.mark.xfail(reason="DATA-05 not yet implemented", strict=False)
def test_graph_embedding_shape(tmp_path):
    """Struc2Vec embedding file exists with shape [N, 128]."""
    pytest.xfail("DATA-05 stub")
```

The fixture arguments (sp500_ohlcv_fixture, ohlcv_wide_fixture, feature_matrix_fixture) match names defined in conftest.py from Task 1.
  </action>
  <verify>
    <automated>cd /Users/gonzaloalonsolidon/Desktop/Repos/Cursor/tfm-stockformer && pytest tests/test_data_pipeline.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest collects all 10 test stubs, all xfail (no errors, no collection failures)</done>
</task>

</tasks>

<verification>
pytest tests/test_data_pipeline.py -x -q reports "10 xfailed" (or similar) with exit code 0.
conftest.py imports cleanly: python -c "import tests.conftest"
No existing Phase 1 tests broken: pytest tests/test_phase1_infra.py -q passes.
</verification>

<success_criteria>
- tests/test_data_pipeline.py exists with 10 stubs covering DATA-01 through DATA-05
- tests/conftest.py has sp500_ohlcv_fixture, ohlcv_wide_fixture, feature_matrix_fixture
- pytest collection succeeds with 0 errors, all stubs xfail
- Phase 1 tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-01-SUMMARY.md`
</output>
